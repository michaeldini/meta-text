// Example styles for Chunks feature
import { makeStyles } from "@mui/styles";

export default makeStyles({
    chunk: {
        margin: "8px 0",
        padding: "12px",
        background: "#f5f5f5",
        borderRadius: "4px",
    },
});
