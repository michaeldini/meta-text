import CompressionToolTab from './CompressionTab';
export * from './CompressionTool';

export { CompressionToolTab };
export { default as CompressionTool } from './CompressionTool';

// this is probably wrong, but it works for now, fix it later when you make all the imports consistent