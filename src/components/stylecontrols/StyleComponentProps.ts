// // Shared props interface for style control components
// // Use this for controlled style inputs (text size, line height, etc.)

// export interface StyleComponentProps {
//     value: number;
//     onChange: (val: number) => void;
//     disabled?: boolean;
// }
